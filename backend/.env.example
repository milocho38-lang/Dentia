APP_NAME=Dentia API
APP_ENV=local
APP_DEBUG=true
API_PREFIX=/api
LOG_LEVEL=INFO

# Prepared for future PostgreSQL configuration.
# C002 does not require a real database connection.
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/dentia
